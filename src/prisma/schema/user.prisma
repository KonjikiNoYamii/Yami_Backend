model User {
    id            Int    @id @default(autoincrement())
    name          String
    email         String @unique
    password_hash String
    deletedAt     DateTime?
    role String @default("USER") 

    orders Order[]

    @@map("users")
}
